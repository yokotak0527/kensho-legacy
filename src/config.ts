const config = {
  customAttrPrefix    : 'k-',
  errorMessageWrapper : 'span',
  verbose             : true,
  errorClassName      : 'kensho-has-error',
  autocomplete        : false,
  HTML5novalidate     : true
}

export default config
